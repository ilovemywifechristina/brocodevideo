#you need to change the strings into floats to use in math, floats because the area might include decimals
length = float(input("enter the length in cm: "))
width = float(input("enter the length in cm: "))
area = length * width

print (f"the area is:  {area}cm²")